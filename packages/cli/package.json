{
  "name": "@decisiongraph/cli",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "decisiongraph": "bin/decisiongraph"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check:run": "node ./bin/decisiongraph --help",
    "test": "vitest run",
    "clean": "tsc -b tsconfig.build.json --clean"
  },
  "dependencies": {
    "@decisiongraph/core": "0.4.1",
    "@decisiongraph/schema": "0.1.0",
    "@decisiongraph/io-json": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
